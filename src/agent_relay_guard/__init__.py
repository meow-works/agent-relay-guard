"""agent-relay-guard: deterministic approval cards from structured agent reports.

The stable public interfaces are the CLI (``agent-relay-guard``) and the JSON
input schema. Python modules in this package are internal and may change
between releases without notice.
"""

__version__ = "0.1.0"
